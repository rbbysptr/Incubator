<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Incubator</title>
</head>
 <%- include('_navbar') %>
<body>
    <div class="container" style="margin-top:50px">
<table class="table">
  
  <thead>
    <tr>
      <th scope="col">Name</th>
      <th scope="col">Code</th>
      <th scope="col">Location</th>
      <th scope="col">Level</th>
      <th scope="col">Action</th>
    </tr>
  </thead>
  <tbody>
    <% data.forEach(element => { %>
    <tr>
      <td><%= element.name %></td>
      <td><%= element.code %></td>
      <td><%= element.location %></td>
      <td><%= element.level %></td>
      <td>
        <a href="/incubators/detail/<%= element.id %>" type="button" class="btn btn-secondary">See Detail</a>
        </td>
    </tr>
    <% }) %>
  </tbody>
</table>
        </section>
         <%- include('_bootstrap') %>
    </div>
   
</body>
</html>